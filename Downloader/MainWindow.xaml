<Window x:Class="UniversalDownloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversalDownloader"
        mc:Ignorable="d"
        Title="Universal File Downloader" Height="650" Width="900" 
        WindowStartupLocation="CenterScreen"
        FontFamily="Inter, Segoe UI, Arial"
        FontWeight="Normal"
        Background="Transparent"
        Loaded="MainWindow_Loaded">

    <Window.Resources>
        <!-- Color Resources -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#6366f1"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#4f46e5"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#8b5cf6"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#06b6d4"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#ffffff"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#f8fafc"/>
        <SolidColorBrush x:Key="CardBrush" Color="#ffffff"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#1e293b"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#64748b"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#e2e8f0"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#f1f5f9"/>

        <!-- Drop Shadow Effect -->
        <DropShadowEffect x:Key="CardShadow" BlurRadius="20" ShadowDepth="4" Color="#000000" Opacity="0.08"/>
        <DropShadowEffect x:Key="ButtonShadow" BlurRadius="8" ShadowDepth="2" Color="#6366f1" Opacity="0.25"/>
        <DropShadowEffect x:Key="WindowShadow" BlurRadius="30" ShadowDepth="8" Color="#000000" Opacity="0.12"/>

        <!-- Gradient Resources -->
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6366f1" Offset="0"/>
            <GradientStop Color="#8b5cf6" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#f8fafc" Offset="0"/>
            <GradientStop Color="#f1f5f9" Offset="1"/>
        </LinearGradientBrush>

        <!-- Modern Button Style -->
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="{StaticResource PrimaryGradient}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect" Value="{StaticResource ButtonShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="12" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                           To="1.05" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                           To="1.05" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                           To="1.0" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                           To="1.0" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#cbd5e1"/>
                                <Setter Property="Foreground" Value="#94a3b8"/>
                                <Setter Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        </Style>

        <!-- Secondary Button Style -->
        <Style TargetType="Button" x:Key="SecondaryButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Effect" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Color="#6366f1" Opacity="0.2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style TargetType="ComboBox" x:Key="ModernComboBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="MaxDropDownHeight" Value="200"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12"
                                    Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <ContentPresenter x:Name="ContentSite" 
                                                    IsHitTestVisible="False" 
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left"/>
                                    <TextBox x:Name="PART_EditableTextBox" 
                                           Visibility="Hidden" 
                                           IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                    <ToggleButton x:Name="ToggleButton" 
                                                Focusable="False"
                                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                ClickMode="Press"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Width="30">
                                        <Path Data="M7,10L12,15L17,10" Stroke="{StaticResource TextSecondaryBrush}" StrokeThickness="2"/>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup x:Name="Popup" 
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Background="{StaticResource SurfaceBrush}"
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="2"
                                            CornerRadius="12"
                                            Effect="{StaticResource CardShadow}">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Progress Bar Style -->
        <Style TargetType="ProgressBar" x:Key="ModernProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Background" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryGradient}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4">
                            <Grid>
                                <Border x:Name="PART_Track" 
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="4"/>
                                <Border x:Name="PART_Indicator" 
                                        Background="{TemplateBinding Foreground}"
                                        CornerRadius="4"
                                        HorizontalAlignment="Left">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="4" ShadowDepth="0" Color="#6366f1" Opacity="0.4"/>
                                    </Border.Effect>
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Label Style -->
        <Style TargetType="TextBlock" x:Key="ModernLabel">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="8,8,8,4"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="SecondaryLabel">
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="8,4"/>
        </Style>
    </Window.Resources>

    <!-- Main Window Content -->
    <Border Background="{StaticResource BackgroundGradient}" 
            CornerRadius="16" 
            Effect="{StaticResource WindowShadow}">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" 
                    Background="{StaticResource PrimaryGradient}" 
                    CornerRadius="16,16,0,0" 
                    Height="50">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0" HorizontalAlignment="Center">
                        <Ellipse Width="12" Height="12" Fill="White" Opacity="0.9" Margin="0,0,8,0"/>
                        <TextBlock Text="Universal File Downloader" 
                                   Foreground="White" 
                                   FontWeight="SemiBold" 
                                   FontSize="16" 
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <Border Background="{StaticResource CardBrush}" 
                        CornerRadius="0,0,16,16" 
                        Padding="40">
                    <StackPanel MaxWidth="600">

                        <!-- Header Section -->
                        <TextBlock Text="Download Any File" 
                                   FontSize="32" 
                                   FontWeight="Bold" 
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   TextAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="Paste any URL and download files with ease" 
                                   Style="{StaticResource SecondaryLabel}"
                                   TextAlignment="Center"
                                   FontSize="16"
                                   Margin="0,0,0,40"/>

                        <!-- URL Input Section -->
                        <Border Background="{StaticResource SurfaceBrush}" 
                                CornerRadius="16" 
                                Padding="24" 
                                Margin="0,0,0,24"
                                Effect="{StaticResource CardShadow}">
                            <StackPanel>
                                <TextBlock Text="🔗 URL" Style="{StaticResource ModernLabel}"/>
                                <TextBox x:Name="UrlTextBox" 
                                         Style="{StaticResource ModernTextBox}"
                                         TextChanged="UrlTextBox_TextChanged"
                                         GotFocus="UrlTextBox_GotFocus" 
                                         LostFocus="UrlTextBox_LostFocus" 
                                         Text="Paste URL here..." 
                                         Foreground="{StaticResource TextSecondaryBrush}"
                                         Margin="0"/>
                            </StackPanel>
                        </Border>

                        <!-- Download Location Section -->
                        <Border Background="{StaticResource SurfaceBrush}" 
                                CornerRadius="16" 
                                Padding="24" 
                                Margin="0,0,0,24"
                                Effect="{StaticResource CardShadow}">
                            <StackPanel>
                                <TextBlock Text="📁 Download Location" Style="{StaticResource ModernLabel}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="DirectoryPathTextBox" 
                                             Grid.Column="0"
                                             Style="{StaticResource ModernTextBox}"
                                             IsReadOnly="True" 
                                             Text="No directory selected" 
                                             Foreground="{StaticResource TextSecondaryBrush}"
                                             Margin="0,0,8,0"/>
                                    <Button x:Name="BrowseButton" 
                                            Grid.Column="1"
                                            Style="{StaticResource SecondaryButton}"
                                            Content="Browse"
                                            Click="BrowseButton_Click"
                                            Margin="0"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Quality Selection Section (YouTube) -->
                        <Border x:Name="QualitySection"
                                Background="{StaticResource SurfaceBrush}" 
                                CornerRadius="16" 
                                Padding="24" 
                                Margin="0,0,0,24"
                                Effect="{StaticResource CardShadow}"
                                Visibility="Collapsed">
                            <StackPanel x:Name="YouTubeQualityPanel">
                                <TextBlock x:Name="YouTubeQualityLabel" 
                                           Text="🎬 Video Quality" 
                                           Style="{StaticResource ModernLabel}"/>
                                <TextBlock Text="Select your preferred video quality and format" 
                                           Style="{StaticResource SecondaryLabel}"
                                           Margin="8,0,8,16"/>
                                <ComboBox x:Name="YouTubeQualityComboBox" 
                                          Style="{StaticResource ModernComboBox}"
                                          DisplayMemberPath="Label" 
                                          SelectedValuePath="FormatCode" 
                                          SelectionChanged="YouTubeQualityComboBox_SelectionChanged"
                                          PreviewMouseLeftButtonDown="YouTubeQualityComboBox_PreviewMouseLeftButtonDown"
                                          Margin="0"/>
                            </StackPanel>
                        </Border>

                        <!-- Download Button -->
                        <Button x:Name="DownloadButton" 
                                Style="{StaticResource ModernButton}"
                                Content="🚀 Start Download"
                                Click="DownloadButton_Click" 
                                IsEnabled="False"
                                HorizontalAlignment="Center"
                                FontSize="16"
                                Padding="32,16"
                                Margin="0,0,0,32"/>

                        <!-- Progress Section -->
                        <Border Background="{StaticResource SurfaceBrush}" 
                                CornerRadius="16" 
                                Padding="24" 
                                Effect="{StaticResource CardShadow}">
                            <StackPanel>
                                <TextBlock x:Name="FileNameTextBlock" 
                                           Text="" 
                                           FontWeight="SemiBold" 
                                           FontSize="16"
                                           Foreground="{StaticResource PrimaryBrush}"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap" 
                                           Margin="0,0,0,16"/>

                                <ProgressBar x:Name="DownloadProgressBar" 
                                             Style="{StaticResource ModernProgressBar}"
                                             Margin="0,0,0,16"/>

                                <TextBlock x:Name="StatusTextBlock" 
                                           Text="Ready to download..." 
                                           Style="{StaticResource SecondaryLabel}"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap"
                                           FontSize="14"
                                           Margin="0"/>
                            </StackPanel>
                        </Border>

                        <!-- Info Footer -->
                        <Border Background="{StaticResource HoverBrush}" 
                                CornerRadius="12" 
                                Padding="20" 
                                Margin="0,32,0,0">
                            <StackPanel>
                                <TextBlock Text="ℹ️ Supported Platforms" 
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           FontSize="14"
                                           Margin="0,0,0,8"/>
                                <TextBlock TextWrapping="Wrap" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="13"
                                           LineHeight="20">
                                    Direct file links, Google Drive, YouTube, and many more platforms are supported. 
                                    The application will automatically download required tools (yt-dlp, ffmpeg) as needed.
                                </TextBlock>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </Border>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>